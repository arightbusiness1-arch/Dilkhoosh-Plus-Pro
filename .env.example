# Environment Variables (Optional)
# All AI Studio environment variables (e.g. GEMINI_API_KEY) are injected automatically at runtime.
# Dilkhoosh Plus app runs fully offline and auto-syncs with Firestore using firebase-applet-config.json.
